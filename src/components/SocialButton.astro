---
import { Image } from "astro:assets";
const { title, link, icon } = Astro.props;
---

<a 
    href={link} 
    class="rounded-full border border-black bg-gray-600 size-10 flex items-center justify-center transform transition duration-700 hover:bg-gray-300 hover:scale-110 hover:rotate-z-360"
    target=”_blank”
>
    <Image
        width="20"
        height="20"
        src={icon} 
        alt={title}
    >
    <span class="sr-only">
        {title}
    </span>
</a>