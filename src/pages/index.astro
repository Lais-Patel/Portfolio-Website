---
import "../styles/global.css";
import { getCollection} from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import { Image } from 'astro:assets';

const allProjects = await getCollection("project_content")
---

<BaseLayout pageTitle="Portfolio">
	<div class="flex justify-center items-center">
		<div class="rounded-xl gap-5 flex justify-center items-center flex-col md:flex-row">
			<div class="">
				<div class="rounded-full bg-[#4A474C] aspect-1/1 w-40 p-1 justify-center items-center md:w-100 md:p-5">
					<Image 
					src="/lais.jpg"
					width="400" height="400" ß
					class="rounded-full aspect-1/1 w-full object-cover transform transition duration-700 hover:scale-111 hover:rotate-z-360"
					alt="Lais Patel"
					loading="eager"
					/>
				</div>
			</div>
			<p class="text-[8vw] font-semibold flex text-center select-none">Lais Patel</p>
		</div>
	</div>
</BaseLayout>